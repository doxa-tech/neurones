<header>
	<nav>
		<% if current_group %>
		<ul>
			<% current_group.pages.order("page_order ASC").each do |page| %>
				<li>
					<%= link_to_active page.name, page_url(page.url) %>
				</li>
			<% end %>
		</ul>
		<% end %>
	</nav>
</header>
