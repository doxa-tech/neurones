<% if !signed_in_user? && request.fullpath != "/login" %>
	<div id="login">
		<div class="big-forms">
			<div class="forms"></div>
		</div>

		<div class="login"> 
			<p><a data-remote="true" href="/login"><i class="foundiconge-down-arrow"></i>Login</a></p>
		</div>
	</div>
<% elsif signed_in_user? %>
	<div id="login">
		<div class="login"> 
			<p><a href="/signout" data-method="delete" rel="nofollow">Déconnecter</a></p>
		</div>
	</div>
<% end %>