<h1>Edition libre</h1>

<%= form_for [:admin, current_group, @style] do |f| %>
	<%= render 'shared/error_messages', object: f.object %>

	<%= f.label :content %>
	<%= f.text_area :content %>

	<%= f.submit %>
<% end %>