<p class="retour"><%= link_to "<i class='foundicong-left-arrow'></i> Retour".html_safe, admin_group_g_images_path(current_group) %></p>

<h1>Ajouter une image</h1>

<%= form_for [:admin, current_group, @image] do |f| %>

	<%= render 'shared/error_messages', object: f.object %>
	<table>
		<tr>
			<td><%= f.label :image %></td>
			<td><%= f.file_field :image %></td>
		</tr>
	</table>
	<div>
		<%= f.submit "Téléverser l'image", class: "w8-button red" %>
	</div>
<% end %>