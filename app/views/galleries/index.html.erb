<% provide(:title, 'Medias photos galerie') %>

<%= render 'shared/image_banner', image: '/assets/medias/banner.jpg' %>

<h1>Medias</h1>
<h2>Photos</h2>

<div id="medias">

	<% @galleries.each do |gallery| %>
	<%= link_to image_tag(gallery.paintings.first.image.url), gallery_path(gallery) %>
	<% end %>
</div>
