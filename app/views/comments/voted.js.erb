$(function () {
  $('#<%= @comment.id %> .comment_vote .popup').hide().fadeIn();
  $('#<%= @comment.id %> .comment_vote').mouseleave(function() {
  	if ($(this).children('.popup').is(':visible')) {
  		$(this).children('.popup').fadeOut();
  	}
  });
});