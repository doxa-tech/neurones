$(function () {
	var comment = $('#<%= @comment.id %>') ;
  comment.find('.comment_vote').find('.popup').hide().fadeIn();
  comment.find('.comment_vote').mouseleave(function() {
  	$(this).find('.popup').fadeOut();
  });
});